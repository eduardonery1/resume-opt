import React from "react"
import Form from "../components/Form"

export default function OptimizerView(props){
    return (
        <main>
            <Form {...props}/>
        </main>
        
    )
}